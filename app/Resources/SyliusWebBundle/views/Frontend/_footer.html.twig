<div class="col-md-3 col-sm-6">
    {% set links = cmf_children(cmf_find('/cms/pages')) %}
    {% if links is not empty %}
        <h4>{{ 'app.ui.pages'|trans }}</h4>
        <ul>
            {% for link in links %}
                {% if cmf_is_linkable(link) %}
                    <li><a href="{{ path(link) }}">{{ link.title|striptags }}</a></li>
                {% endif %}
            {% endfor %}
        </ul>
        <hr/>
    {% endif %}

    {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <h4>{{ 'app.ui.user_section'|trans }}</h4>
        <ul>
            <li><a href="{{ path('sylius_user_security_login') }}">{{ 'app.ui.log_in'|trans }}</a></li>
            <li><a href="{{ path('sylius_user_registration') }}">{{ 'sylius.ui.register'|trans }}</a></li>
        </ul>
    {% else %}
        <h4 class="hidden-md hidden-lg">{{ app.user.email }}</h4>
        <h6 class="visible-md">{{ app.user.email }}</h6>
        <h5 class="visible-lg">{{ app.user.email }}</h5>
        <ul>
            <li><a href="{{ path('sylius_account_profile_update') }}">{{ 'app.ui.personal_information'|trans }}</a></li>
            <li><a href="{{ path('sylius_account_order_index') }}">{{ 'app.ui.view_orders'|trans }}</a></li>
            <li><a href="{{ path('sylius_user_security_logout') }}">{{ 'app.ui.log_out'|trans }}</a></li>
        </ul>
    {% endif %}

    <hr class="hidden-md hidden-lg">
</div>

<div class="col-md-3 col-sm-6">
    <h4>Top categories</h4>

    <h5>Men</h5>
    <ul>
        <li><a href="#">T-shirts</a>
        </li>
        <li><a href="#">Shirts</a>
        </li>
        <li><a href="#">Accessories</a>
        </li>
    </ul>

    <h5>Ladies</h5>
    <ul>
        <li><a href="#">T-shirts</a>
        </li>
        <li><a href="#">Skirts</a>
        </li>
        <li><a href="#">Pants</a>
        </li>
        <li><a href="#">Accessories</a>
        </li>
    </ul>

    <hr class="hidden-md hidden-lg">
</div>

<div class="col-md-3 col-sm-6">
    <h4>Where to find us</h4>

    <p><strong>Obaju Ltd.</strong>
        <br>13/25 New Avenue
        <br>New Heaven
        <br>45Y 73J
        <br>England
        <br>
        <strong>Great Britain</strong>
    </p>

    <a href="#">Go to contact page</a>
    <hr class="hidden-md hidden-lg hidden-sm">
</div>

<div class="col-md-3 col-sm-6">
    <h4>Get the news</h4>
    <p class="text-muted">Pellentesque habitant morbi tristique senectus et netus et malesuada fames ac turpisegestas.</p>

    {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <form action="{{ path('app_customer_newsletter_subscribe_guest') }}" method="post" name="subscribe_form">
            <div class="input-group">
                <input type="text" class="form-control" name="sylius_customer_guest[email]">
                <input type="hidden" name="sylius_customer_guest[_token]" value="{{ csrf_token('subscribe_form') }}">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit">{{ 'app.ui.subscribe'|trans }}</button>
                </span>
            </div>
        </form>
    {% else %}
        <form action="{{ path('app_customer_newsletter_subscribe_logged') }}" method="post">
            <button class="btn btn-primary" type="submit">{{ 'app.ui.subscribe'|trans }}</button>
        </form>
    {% endif %}

    <hr>

    <h4>Stay in touch</h4>
    <p class="social">
        <a href="#"><i class="fa fa-facebook-square"></i></a>
        <a href="#"><i class="fa fa-instagram"></i></a>
        <a href="#"><i class="fa fa-google-plus"></i></a>
        <a href="#"><i class="fa fa-envelope"></i></a>
    </p>
</div>
